name: Build Mod JAR

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
        
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4
      
    - name: Build with Gradle
      working-directory: ./Donut-Algorithm
      run: |
        chmod +x gradlew
        ./gradlew build --no-daemon -Porg.gradle.dependency.verification=off 2>&1 | tail -50
        
    - name: List build output
      working-directory: ./Donut-Algorithm
      run: ls -la build/libs/ 2>/dev/null || echo "No JAR found"
        
    - name: Upload JAR artifact
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: donut-algorithm-mod
        path: Donut-Algorithm/build/libs/*.jar
        retention-days: 30
